<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hina.eaglee.mapper.TaskRuleMapper">

    <!-- 根据规则类型查询任务规则列表 -->
    <select id="selectByRuleType" resultType="com.hina.eaglee.entity.TaskRuleEntity">
        SELECT * FROM task_rule 
        WHERE rule_type = #{ruleType} 
        AND deleted = false
        ORDER BY priority DESC, ct DESC
    </select>

    <!-- 根据应用类型查询任务规则列表 -->
    <select id="selectByApplicationType" resultType="com.hina.eaglee.entity.TaskRuleEntity">
        SELECT * FROM task_rule 
        WHERE application_type = #{applicationType} 
        AND deleted = false
        ORDER BY priority DESC, ct DESC
    </select>

    <!-- 查询启用的任务规则列表，按优先级排序 -->
    <select id="selectEnabledRulesOrderByPriority" resultType="com.hina.eaglee.entity.TaskRuleEntity">
        SELECT * FROM task_rule 
        WHERE enabled = true 
        AND deleted = false
        ORDER BY priority DESC, ct DESC
    </select>

    <!-- 根据规则类型和应用类型查询任务规则列表 -->
    <select id="selectByRuleTypeAndApplicationType" resultType="com.hina.eaglee.entity.TaskRuleEntity">
        SELECT * FROM task_rule 
        WHERE rule_type = #{ruleType} 
        AND application_type = #{applicationType}
        AND deleted = false
        ORDER BY priority DESC, ct DESC
    </select>

    <!-- 批量更新规则启用状态 -->
    <update id="updateEnabledStatusByIds">
        UPDATE task_rule 
        SET enabled = #{enabled}, ut = NOW()
        WHERE task_rule_id IN
        <foreach collection="ruleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND deleted = false
    </update>

    <!-- 更新规则优先级 -->
    <update id="updatePriorityById">
        UPDATE task_rule 
        SET priority = #{priority}, ut = NOW()
        WHERE task_rule_id = #{taskRuleId} 
        AND deleted = false
    </update>

    <!-- 批量插入任务规则 -->
    <insert id="insertBatch">
        INSERT INTO task_rule (
            rule_name, rule_type, rule_condition, rule_action, 
            application_type, priority, enabled, rule_description, 
            ct, ut, cid, deleted
        ) VALUES
        <foreach collection="taskRules" item="rule" separator=",">
            (
                #{rule.ruleName}, #{rule.ruleType}, #{rule.ruleCondition}, 
                #{rule.ruleAction}, #{rule.applicationType}, #{rule.priority}, 
                #{rule.enabled}, #{rule.ruleDescription}, #{rule.ct}, 
                #{rule.ut}, #{rule.cid}, #{rule.deleted}
            )
        </foreach>
    </insert>

    <!-- 逻辑删除任务规则 -->
    <update id="logicalDeleteByIds">
        UPDATE task_rule 
        SET deleted = true, ut = NOW()
        WHERE task_rule_id IN
        <foreach collection="ruleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>

    <!-- 物理删除任务规则 -->
    <delete id="physicalDeleteByIds">
        DELETE FROM task_rule 
        WHERE task_rule_id IN
        <foreach collection="ruleIds" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

    <!-- 查询最大优先级值 -->
    <select id="selectMaxPriority" resultType="java.lang.Integer">
        SELECT MAX(priority) FROM task_rule 
        WHERE deleted = false
    </select>

</mapper>